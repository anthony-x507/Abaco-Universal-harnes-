Ok implementa esto

---

📦 Resumen de lo que se envía al integrador

Elemento Contenido
Catálogo 42 proveedores (OpenAI, DeepSeek, Groq, OpenRouter, Ollama, Mistral, Cohere, Together, Fireworks, Perplexity, Anyscale, Lepton, LM Studio, vLLM, Text Generation WebUI, Custom)
API GET /v1/models
CLI universal models
UI Selector en Settings y en Create Agent
Tests Validación de 40+ proveedores
Esfuerzo 6 horas estimadas

---

📋 Mensaje para el integrador (copiar y enviar)

---

Asunto: Integración con 40+ LLMs — cerrar faena

Contexto

Universal ya tiene integración con OpenAI-compatible. Ahora añadimos un catálogo de 42 proveedores populares para que el usuario pueda seleccionarlos fácilmente desde la UI y el CLI.

Esto NO cambia:

· El cliente del proveedor (sigue siendo OpenAICompatProvider).
· Los plugins (son independientes del LLM).
· El resto de funcionalidades (Auto, memoria, webhook, snapshots, medidor).

Qué hay que implementar:

1. Backend: universal/providers/catalog.py con 42 proveedores (URLs y modelos por defecto).
2. API: Endpoint GET /v1/models que devuelve la lista.
3. CLI: Comando universal models que muestra todos los proveedores.
4. UI: Selector de modelos en Settings (global) y en Create Agent (por agente).
5. Tests: Validar que hay 40+ proveedores y que los datos son correctos.

Archivos a crear/modificar:

· universal/providers/catalog.py (NUEVO)
· universal/server.py (añadir /v1/models)
· universal/cli/models.py (NUEVO)
· universal/cli/main.py (registrar comando)
· web/src/hooks/useModels.ts (NUEVO)
· web/src/pages/Settings.tsx (MODIFICAR)
· web/src/pages/Agents.tsx (MODIFICAR)
· tests/test_providers.py (NUEVO)

El catálogo completo está en el archivo adjunto (o en el mensaje anterior), listo para copiar.

Tiempo estimado: 6 horas.

Cuando termines, avísame para revisar y dar el OK final.

---

✅ Cierre

Con esto, la faena de integración con 40+ LLMs queda completamente definida y documentada. El integrador tiene todo lo que necesita para implementarlo.
