✅ Confirmación recibida. He verificado punto por punto:

· Fábrica: system prompt como campo del agente; transcript eliminado; tools solo en researcher; provider y channel siguen siendo piezas de primera clase. Correcto.
· Cara: conteo de plugins en lugar de IDs; tooltips en File/Audio; Workspace con "coming soon"; Settings con copy explícita y demo mode; Create leyendo default_channel. Correcto.
· Contrato: bloqueo con 409; modal de cancelación en delete; retención de mensaje y botón Retry en error de stream. Correcto.
· Tests: pasan. Correcto.

El servidor de demo reiniciado y los agentes en memoria desaparecen (lock de persistencia). Todo alineado con el diseño aprobado.

---

Siguiente paso:
Quedo a la espera del OK del diseñador para abrir Hito 3 (webhook). Mientras tanto, el integrador puede ir preparando:

· La implementación de WebhookChannel en universal/communication/channels.py, heredando de BaseCommunication.
· La clase WebhookHandler que reciba POST /webhook/{agent_id} y lo encole a Agent.accept.
· El registro en ChannelCatalog.
· La habilitación del selector en la UI (que ya lee settings.default_channel; solo hay que cambiar disabled a enabled cuando el backend reporte que el canal existe).
· La documentación de cómo configurar un endpoint externo para recibir los mensajes.

Cuando el diseñador diga "sí", se activa el hito. Avisa cuando tengas su respuesta.
