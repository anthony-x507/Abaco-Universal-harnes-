We'll continue with Step 2: Frontend Stack Setup and build the foundation for the custom Aegis UI.

---

🛠️ Frontend Stack: React + TypeScript + Vite + Tailwind

We'll create a new frontend project inside ui/aegis-ui/. The scaffold includes:

· Vite config 
· Tailwind setup 
· Basic folder structure 
· API client 
· Main App shell with routing (react-router) 
· Placeholder pages for Chat, Agents, Browser, Plugins, Scheduler, Settings

We'll keep it modular so we can flesh out each page later.

---

📁 ui/aegis-ui/package.json

{
  "name": "aegis-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "typescript": "^5.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0",
    "tailwindcss": "^3.4.10",
    "postcss": "^8.4.41",
    "autoprefixer": "^10.4.20",
    "lucide-react": "^0.427.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0"
  }
}


---

📁 ui/aegis-ui/vite.config.ts

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/v1': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
});


---

📁 ui/aegis-ui/tailwind.config.js

/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        bg: '#0B0E14',
        surface1: '#131720',
        surface2: '#1A202A',
        surface3: '#222A35',
        border: '#2A3441',
        accent: '#00E5FF',
        accent2: '#A78BFA',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
    },
  },
  plugins: [],
};


---

📁 ui/aegis-ui/postcss.config.js

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};


---

📁 ui/aegis-ui/index.html

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Aegis Agent Factory</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>


---

📁 ui/aegis-ui/src/main.tsx

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);


---

📁 ui/aegis-ui/src/index.css

@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-bg text-gray-100 font-sans;
}


---

📁 ui/aegis-ui/src/App.tsx

import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom';
import ChatPage from './pages/ChatPage';
import AgentsPage from './pages/AgentsPage';
import BrowserPage from './pages/BrowserPage';
import PluginsPage from './pages/PluginsPage';
import SchedulerPage from './pages/SchedulerPage';
import SettingsPage from './pages/SettingsPage';
import Sidebar from './components/Sidebar';
import TopBar from './components/TopBar';

function App() {
  return (
    <Router>
      <div className="flex h-screen">
        <Sidebar />
        <div className="flex flex-col flex-1">
          <TopBar />
          <main className="flex-1 overflow-auto">
            <Routes>
              <Route path="/" element={<ChatPage />} />
              <Route path="/agents" element={<AgentsPage />} />
              <Route path="/browser" element={<BrowserPage />} />
              <Route path="/plugins" element={<PluginsPage />} />
              <Route path="/scheduler" element={<SchedulerPage />} />
              <Route path="/settings" element={<SettingsPage />} />
            </Routes>
          </main>
        </div>
      </div>
    </Router>
  );
}

export default App;


---

📁 ui/aegis-ui/src/components/Sidebar.tsx

import { Link, useLocation } from 'react-router-dom';
import { MessageSquare, Bot, Globe, Puzzle, Calendar, Settings } from 'lucide-react';

const navItems = [
  { path: '/', label: 'Chat', icon: MessageSquare },
  { path: '/agents', label: 'Agents', icon: Bot },
  { path: '/browser', label: 'Browser', icon: Globe },
  { path: '/plugins', label: 'Plugins', icon: Puzzle },
  { path: '/scheduler', label: 'Scheduler', icon: Calendar },
  { path: '/settings', label: 'Settings', icon: Settings },
];

export default function Sidebar() {
  const location = useLocation();
  return (
    <aside className="w-16 bg-surface1 border-r border-border flex flex-col items-center py-4">
      <div className="text-accent text-2xl font-bold mb-6">A</div>
      <nav className="flex flex-col gap-4">
        {navItems.map(({ path, label, icon: Icon }) => (
          <Link
            key={path}
            to={path}
            className={`p-2 rounded-lg hover:bg-surface2 ${location.pathname === path ? 'text-accent' : 'text-gray-400'}`}
            title={label}
          >
            <Icon size={20} />
          </Link>
        ))}
      </nav>
    </aside>
  );
}


---

📁 ui/aegis-ui/src/components/TopBar.tsx

export default function TopBar() {
  return (
    <header className="h-12 bg-surface1 border-b border-border flex items-center px-4">
      <div className="text-sm text-gray-400">Aegis Agent Factory</div>
      <div className="ml-auto flex items-center gap-3">
        <span className="w-2 h-2 rounded-full bg-green-500 animate-pulse" />
        <span className="text-xs text-gray-300">Connected</span>
      </div>
    </header>
  );
}


---

📁 ui/aegis-ui/src/pages/ChatPage.tsx

export default function ChatPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold mb-4">Chat</h1>
      <div className="bg-surface1 border border-border rounded-lg p-4 h-[calc(100vh-8rem)]">
        {/* Chat messages and input will be implemented later */}
        <p className="text-gray-500">Chat interface coming soon.</p>
      </div>
    </div>
  );
}


---

📁 ui/aegis-ui/src/pages/AgentsPage.tsx

export default function AgentsPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold mb-4">Agents</h1>
      <div className="bg-surface1 border border-border rounded-lg p-4">
        <p className="text-gray-500">Agent management coming soon.</p>
      </div>
    </div>
  );
}


---

📁 ui/aegis-ui/src/pages/BrowserPage.tsx

export default function BrowserPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold mb-4">Browser Automation</h1>
      <div className="bg-surface1 border border-border rounded-lg p-4">
        <p className="text-gray-500">Browser tools coming soon.</p>
      </div>
    </div>
  );
}


---

📁 ui/aegis-ui/src/pages/PluginsPage.tsx

export default function PluginsPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold mb-4">Plugins</h1>
      <div className="bg-surface1 border border-border rounded-lg p-4">
        <p className="text-gray-500">Plugin manager coming soon.</p>
      </div>
    </div>
  );
}


---

📁 ui/aegis-ui/src/pages/SchedulerPage.tsx

export default function SchedulerPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold mb-4">Scheduler</h1>
      <div className="bg-surface1 border border-border rounded-lg p-4">
        <p className="text-gray-500">Scheduler coming soon.</p>
      </div>
    </div>
  );
}


---

📁 ui/aegis-ui/src/pages/SettingsPage.tsx

export default function SettingsPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold mb-4">Settings</h1>
      <div className="bg-surface1 border border-border rounded-lg p-4">
        <p className="text-gray-500">Settings coming soon.</p>
      </div>
    </div>
  );
}


---

That's the base scaffold. The next steps will implement each page in detail, starting with the Chat interface.
