Mensaje para el integrador: Avisos efímeros de actividad del agente (tool execution y delegación).

Avisos de 2–3 s encima del compositor. No van al historial. SSE type=tool_execution | delegating | token | done.

Lock-safe mapping: no `universal/agent.py`, no `useChat.ts`, no `Chat.tsx`. Events go through `accept_stream_events` → bound channel. Delegation is a status event only; no second agent call.
