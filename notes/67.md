Asunto: Instrucciones para el integrador — actualización automática y plugins persistentes

Objetivo: native plugins always installed; self-contained macOS app; self-update from GitHub; plugins persist across updates.

Owner sketches (not copied as-is):

- `universal/settings.py` with Application Support plugins/ + memory/ — implemented as `universal/paths.py` (no clash with `Settings`).
- Copy `*.py` into `~/Library/Application Support/Universal/plugins/` and load them — **not done**. Filesystem plugin discovery stays deferred. Manifest + registry + memory persist; code stays in the package.
- `requests` updater that `rm -rf /Applications/Universal.app` and `sys.exit` — implemented as `universal/updater.py` with `httpx`, version tuples, GitHub https-only assets, apply only when frozen Darwin or `UNIVERSAL_UPDATE_ALLOW_INSTALL=1`.
- `app.py` on port 43123 + `ensure_plugins_installed` copy — desktop stays on 43124 / existing `universal.desktop`.
- YAML templates / class-level `PluginCatalog.register` / `requirements.txt` + Whisper — already rejected in notes 65–66.
- GitHub Action on Python 3.10 + `requirements.txt` — shipped as `.github/workflows/release.yml` (Python 3.12, `pip install -e ".[desktop]"`, no Whisper).

`version.json` matches `_version.py` (`0.1.0`), not a fake 1.0.0.

---

Fin de las instrucciones. Implementar y confirmar.
