We'll now implement Step 8: Settings Page. This page will allow users to configure basic UI preferences (default model, API base URL, theme) and test the API connection. These settings are stored locally in the browser.

---

1. Create src/pages/SettingsPage.tsx

import { useState, useEffect } from 'react';
import { fetchJSON } from '../lib/api';

interface Settings {
  defaultModel: string;
  apiBaseUrl: string;
  theme: 'dark' | 'light';
  autoScroll: boolean;
}

const defaultSettings: Settings = {
  defaultModel: 'gpt-4o-mini',
  apiBaseUrl: '/v1',
  theme: 'dark',
  autoScroll: true,
};

export default function SettingsPage() {
  const [settings, setSettings] = useState<Settings>(() => {
    const saved = localStorage.getItem('aegis-settings');
    return saved ? { ...defaultSettings, ...JSON.parse(saved) } : defaultSettings;
  });
  const [apiStatus, setApiStatus] = useState<string>('');

  useEffect(() => {
    localStorage.setItem('aegis-settings', JSON.stringify(settings));
  }, [settings]);

  const handleChange = (key: keyof Settings, value: any) => {
    setSettings((prev) => ({ ...prev, [key]: value }));
  };

  const testApi = async () => {
    setApiStatus('Testing...');
    try {
      const health = await fetchJSON('/health');
      setApiStatus(`Connected: ${JSON.stringify(health)}`);
    } catch (e) {
      setApiStatus(`Error: ${(e as Error).message}`);
    }
  };

  return (
    <div className="p-4 max-w-2xl">
      <h1 className="text-xl font-semibold mb-4">Settings</h1>

      <div className="bg-surface1 border border-border rounded-lg p-6 space-y-6">
        <div>
          <h2 className="text-lg font-medium mb-3">General</h2>
          <div className="space-y-4">
            <div>
              <label className="block text-sm text-gray-400 mb-1">Default Model</label>
              <input
                type="text"
                value={settings.defaultModel}
                onChange={(e) => handleChange('defaultModel', e.target.value)}
                className="w-full bg-surface2 border border-border rounded px-3 py-2 text-sm"
                placeholder="gpt-4o-mini"
              />
            </div>
            <div>
              <label className="block text-sm text-gray-400 mb-1">API Base URL</label>
              <input
                type="text"
                value={settings.apiBaseUrl}
                onChange={(e) => handleChange('apiBaseUrl', e.target.value)}
                className="w-full bg-surface2 border border-border rounded px-3 py-2 text-sm"
                placeholder="/v1"
              />
            </div>
            <div className="flex items-center gap-2">
              <input
                type="checkbox"
                checked={settings.autoScroll}
                onChange={(e) => handleChange('autoScroll', e.target.checked)}
                className="accent-accent"
              />
              <label className="text-sm">Auto-scroll on new messages</label>
            </div>
          </div>
        </div>

        <div>
          <h2 className="text-lg font-medium mb-3">Appearance</h2>
          <div className="flex gap-4">
            <label className="flex items-center gap-2">
              <input
                type="radio"
                name="theme"
                value="dark"
                checked={settings.theme === 'dark'}
                onChange={() => handleChange('theme', 'dark')}
                className="accent-accent"
              />
              Dark
            </label>
            <label className="flex items-center gap-2">
              <input
                type="radio"
                name="theme"
                value="light"
                checked={settings.theme === 'light'}
                onChange={() => handleChange('theme', 'light')}
                className="accent-accent"
              />
              Light
            </label>
          </div>
        </div>

        <div>
          <h2 className="text-lg font-medium mb-3">API Connection</h2>
          <button
            onClick={testApi}
            className="bg-accent text-black font-medium rounded-lg px-4 py-2 hover:opacity-90"
          >
            Test Connection
          </button>
          {apiStatus && <div className="mt-2 text-sm text-gray-400">{apiStatus}</div>}
        </div>
      </div>
    </div>
  );
}


---

2. Update src/lib/api.ts to use configurable base URL?

Currently API_BASE is hardcoded to /v1. To support the settings apiBaseUrl, we would need to adjust the API client to read from localStorage. We can do that easily:

Modify src/lib/api.ts:

const getApiBase = () => {
  const saved = localStorage.getItem('aegis-settings');
  if (saved) {
    const settings = JSON.parse(saved);
    if (settings.apiBaseUrl) return settings.apiBaseUrl;
  }
  return '/v1';
};

export async function fetchJSON<T = any>(url: string, options: RequestInit = {}): Promise<T> {
  const base = getApiBase();
  const response = await fetch(`${base}${url}`, {
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
    ...options,
  });
  if (!response.ok) {
    const error = await response.text();
    throw new Error(error);
  }
  return response.json();
}

But note that streaming and upload functions also use API_BASE. We can update those as well. Since this is a small change, we can mention it.

We'll also need to ensure other components (ChatPage, etc.) use the same base; they all import from api.ts, so if we change fetchJSON and streamChat, it will be consistent. But uploadFile uses API_BASE directly; we need to update it too. For now, we can leave as is and note that settings is for future implementation. For the current version, we'll keep hardcoded /v1.

But to be thorough, we can output the updated api.ts with the dynamic base.

Let's produce the updated api.ts as an additional file change.

---

3. Updated src/lib/api.ts (full)

We'll replace the top part with dynamic base and update all functions to use it. We'll output the entire file again for clarity, but it's long. Since we've already provided many functions, maybe we just provide the diff? But the user expects a full code block? The conversation pattern is to give full files when needed. However, it's huge. We can provide only the changed part, but user may want to copy-paste. We'll provide the full file content to avoid confusion, but it might be very long. We can summarize and note that the only change is the base URL retrieval. But to be safe, we can output the updated api.ts with all functions, but that would be excessively long. The previous version already contains all functions, and we can just mention that we need to change const API_BASE = '/v1'; to const getApiBase = ... and use it. That's enough for the user to understand.

Given the context, the user is building the UI, and we are providing files incrementally. We'll provide the SettingsPage code and a note about the API base change. That's sufficient.

We'll also mention that next we could implement resizable panels and global activity indicator, but we already have ActivityIndicator component used in ChatPage. Maybe we need to add global activity indicator to all pages? But not necessary now.

Thus, for this step, we'll output the SettingsPage and the note about API base.

Let's proceed.We'll now implement Step 8: Settings Page. This page allows users to configure basic preferences (default model, API base URL, theme, auto-scroll) and test the API connection. Settings are stored in localStorage.

---

📄 src/pages/SettingsPage.tsx

import { useState, useEffect } from 'react';
import { fetchJSON } from '../lib/api';

interface Settings {
  defaultModel: string;
  apiBaseUrl: string;
  theme: 'dark' | 'light';
  autoScroll: boolean;
}

const defaultSettings: Settings = {
  defaultModel: 'gpt-4o-mini',
  apiBaseUrl: '/v1',
  theme: 'dark',
  autoScroll: true,
};

export default function SettingsPage() {
  const [settings, setSettings] = useState<Settings>(() => {
    const saved = localStorage.getItem('aegis-settings');
    return saved ? { ...defaultSettings, ...JSON.parse(saved) } : defaultSettings;
  });
  const [apiStatus, setApiStatus] = useState<string>('');

  useEffect(() => {
    localStorage.setItem('aegis-settings', JSON.stringify(settings));
  }, [settings]);

  const handleChange = (key: keyof Settings, value: any) => {
    setSettings((prev) => ({ ...prev, [key]: value }));
  };

  const testApi = async () => {
    setApiStatus('Testing...');
    try {
      const health = await fetchJSON('/health');
      setApiStatus(`Connected: ${JSON.stringify(health)}`);
    } catch (e) {
      setApiStatus(`Error: ${(e as Error).message}`);
    }
  };

  return (
    <div className="p-4 max-w-2xl">
      <h1 className="text-xl font-semibold mb-4">Settings</h1>

      <div className="bg-surface1 border border-border rounded-lg p-6 space-y-6">
        <div>
          <h2 className="text-lg font-medium mb-3">General</h2>
          <div className="space-y-4">
            <div>
              <label className="block text-sm text-gray-400 mb-1">Default Model</label>
              <input
                type="text"
                value={settings.defaultModel}
                onChange={(e) => handleChange('defaultModel', e.target.value)}
                className="w-full bg-surface2 border border-border rounded px-3 py-2 text-sm"
                placeholder="gpt-4o-mini"
              />
            </div>
            <div>
              <label className="block text-sm text-gray-400 mb-1">API Base URL</label>
              <input
                type="text"
                value={settings.apiBaseUrl}
                onChange={(e) => handleChange('apiBaseUrl', e.target.value)}
                className="w-full bg-surface2 border border-border rounded px-3 py-2 text-sm"
                placeholder="/v1"
              />
            </div>
            <div className="flex items-center gap-2">
              <input
                type="checkbox"
                checked={settings.autoScroll}
                onChange={(e) => handleChange('autoScroll', e.target.checked)}
                className="accent-accent"
              />
              <label className="text-sm">Auto-scroll on new messages</label>
            </div>
          </div>
        </div>

        <div>
          <h2 className="text-lg font-medium mb-3">Appearance</h2>
          <div className="flex gap-4">
            <label className="flex items-center gap-2">
              <input
                type="radio"
                name="theme"
                value="dark"
                checked={settings.theme === 'dark'}
                onChange={() => handleChange('theme', 'dark')}
                className="accent-accent"
              />
              Dark
            </label>
            <label className="flex items-center gap-2">
              <input
                type="radio"
                name="theme"
                value="light"
                checked={settings.theme === 'light'}
                onChange={() => handleChange('theme', 'light')}
                className="accent-accent"
              />
              Light
            </label>
          </div>
        </div>

        <div>
          <h2 className="text-lg font-medium mb-3">API Connection</h2>
          <button
            onClick={testApi}
            className="bg-accent text-black font-medium rounded-lg px-4 py-2 hover:opacity-90"
          >
            Test Connection
          </button>
          {apiStatus && <div className="mt-2 text-sm text-gray-400">{apiStatus}</div>}
        </div>
      </div>
    </div>
  );
}


---

🔧 Note: Dynamic API Base URL

The api.ts currently hardcodes /v1. To support the setting apiBaseUrl, update api.ts as follows:

Replace:

const API_BASE = '/v1';

with:

const getApiBase = () => {
  const saved = localStorage.getItem('aegis-settings');
  if (saved) {
    const settings = JSON.parse(saved);
    if (settings.apiBaseUrl) return settings.apiBaseUrl;
  }
  return '/v1';
};

Then update all fetch calls in api.ts to use getApiBase() instead of API_BASE. For example:

const response = await fetch(`${getApiBase()}${url}`, ...);

This will make the UI respect the configured API base URL. Note that during development, the default /v1 works with the Vite proxy.

---

✅ Summary

· Settings page implemented with general, appearance, and API connection testing. 
· Settings persisted in localStorage. 
· Dynamic API base URL support noted.

Next, we can focus on global UI polish: resizable panels, command palette, and more responsive layout. Let me know if you want to continue with those refinements.
