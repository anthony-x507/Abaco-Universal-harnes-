Perfecto. Preparo el paquete completo para que el integrador implemente plugins nativos en el Universal Harness y empaquetado nativo para macOS. Esto incluye que cuando se descargue e instale el harness, ya tenga todas las capacidades de fábrica.

---

📦 PAQUETE COMPLETO: PLUGINS NATIVOS + EMPAQUETADO NATIVO macOS

Estructura final del proyecto

```
universal/
├── app.py                 # Punto de entrada PyWebView
├── universal/
│   ├── server.py
│   ├── agent.py
│   ├── factory/
│   │   ├── generator.py
│   │   └── templates/
│   │       ├── general.yaml
│   │       ├── researcher.yaml
│   │       └── coder.yaml
│   └── plugins/
│       ├── terminal.py
│       ├── tts.py
│       ├── stt.py
│       ├── vision.py
│       ├── web_search.py
│       ├── scraper.py
│       └── catalog.py
├── web/
│   └── dist/              # Build de React
├── scripts/
│   ├── build_macos.sh     # Script de empaquetado
│   └── create_dmg.sh      # Script para crear .dmg
└── README.md
```

The sketches that follow (dict-shaped `tools()`, `from universal.plugins import Plugin`, YAML templates, `os.system` TTS, `get_current_provider`, hard `import whisper`, `requests`) are archived here as the owner packet. They were **not** copied into `universal/`. Native plugins already live on the real Plugin protocol (`notes/65.md`). Desktop packaging uses `universal/desktop.py` + root `app.py` around the existing factory (no `universal/agent.py`, no `universal/factory/templates/*.yaml`).

---

🧩 PLUGINS NATIVOS (owner sketches — do not paste as-is)

1–6. terminal / tts / stt / vision / web_search / scraper as in the owner message (`terminal_access` / `execute_command`, `os.system` say, Whisper required, DuckDuckGo via requests).

7. Register on PluginCatalog as class-level `register` (the real catalog is an instance).

8. YAML templates under `universal/factory/templates/` (the real templates are `universal/templates/catalog.py`).

---

🍎 EMPAQUETADO NATIVO macOS (owner sketches)

9. `app.py` — thread `run_server(host="127.0.0.1", port=43123)` + `webview.create_window` on that URL.

Integrator cut: window loads the **factory** on `127.0.0.1:43124` after `web/dist` is mounted on the same app. Port 43123 stays Vite in development.

10. `scripts/build_macos.sh` — bun build + PyInstaller `--onefile --windowed` with `--hidden-import=whisper` and `--hidden-import=requests`.

Integrator cut: onedir `.app` on Darwin; no Whisper in the bundle; Linux runs `--check` only.

11. `scripts/create_dmg.sh` — `hdiutil` from `Universal.app`.

---

🔧 DEPENDENCIAS (owner)

```
webview
pyinstaller
openai-whisper
beautifulsoup4
requests
```

Integrator cut: `beautifulsoup4` already required; `pywebview` is extra `[desktop]`; Whisper stays `[media]`; no `requests` (httpx).

---

✅ CRITERIOS DE ACEPTACIÓN

□ Todos los plugins nativos están registrados y disponibles en todo agente.
□ universal create crea agentes con los 6 plugins.
□ universal ask permite ejecutar comandos, TTS, STT, visión, búsqueda y scraping.
□ app.py abre una ventana nativa con la SPA.
□ build_macos.sh genera Universal.app.
□ create_dmg.sh genera Universal.dmg.
□ El .app se abre correctamente en macOS.
□ El servidor se inicia automáticamente al abrir la app.
□ Todo funciona offline (excepto LLM API y búsqueda web).

---

Fin del paquete. Enviar al integrador.
